{
  "name": "card-gallery",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "check:cards": "node scripts/card-data.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "pnpm build && node scripts/test-browser.mjs && node scripts/test-studio-browser.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "playwright": "1.61.1",
    "sharp": "0.35.4",
    "three": "0.169.0",
    "yaml": "2.9.1"
  }
}
