# 卡间拾光视觉改版验收

当前配色以文末的「webapps 风格补充验收」为准；此前的暖色方案与 9 张卡片测试数据保留为历史记录。

依据用户提供的《「卡间拾光」视觉设计改版说明》（`card-shiguang-visual-redesign.md`），于 2026-09-17 完成本地实现与验收。

## 实现范围

| 文件 | 修改内容 |
| --- | --- |
| `index.html` | 合并重复站名，保留首页链接；采用文档建议的介绍；增加轻量页脚；为现有弹层分出关闭区与滚动内容区；同步浏览器主题色。 |
| `styles.css` | 统一暖白、浅灰、炭黑与暗朱红变量；宋体品牌标题与系统黑体正文；1160px 内容宽度及统一对齐线；开放式收藏条目与 4:3 展台；卡面完整展示；响应式控件、三列/两列/单列画廊；焦点与减少动态效果样式。 |
| `app.js` | 用已有字段展示银行、卡种和卡组织；保留卡片按钮的可访问名称；弹层随可视窗口限制尺寸并让内容滚动，替换旧的按标题高度不断缩窄面板的计算。 |
| `README.md` | 更新视觉规范、图片比例及大图展示说明。 |

卡片资料、原图、图片地址、排序、搜索匹配、筛选条件、首页路由、键盘快捷键和弹层开关事件保持原有实现。未新增依赖、UI 库、在线字体或业务功能。

## 构建与现有测试

| 检查 | 结果 |
| --- | --- |
| `pnpm test` | 27 项通过，0 失败。 |
| `pnpm check:cards` | 9 张卡片资料通过。 |
| `NODE_USE_ENV_PROXY=1 pnpm build` | 9 张真实卡面构建成功，生成 `public/`。 |
| `node --check app.js` | 通过。 |
| `git diff --check` | 通过。 |

首次直接构建时，现有 Imgur 图片 `cmb-koi-debit` 的网络请求失败；使用本机已有代理后，完整构建成功。未替换图片或修改图片来源，也未将代理配置写入项目。

## 浏览器验收

使用本机 Chrome 150.0.7871.128、Playwright 和实际构建目录进行检查。下表宽度为 CSS 视口宽度；桌面系统保留滚动条槽，因此内容宽度会扣除滚动条空间。

| 视口宽度 | 列数 | 行间距 | 页面、控件、弹层横向溢出 |
| --- | --- | --- | --- |
| 1440px | 3 | 40px | 无 |
| 1024px | 3 | 40px | 无 |
| 1023px | 2 | 36px | 无 |
| 768px | 2 | 36px | 无 |
| 600px | 2 | 36px | 无 |
| 599px | 1 | 32px | 无 |
| 390px | 1 | 32px | 无 |
| 375px | 1 | 32px | 无 |
| 320px | 1 | 32px | 无 |

- 标题、工具栏、结果说明、画廊、页脚左边缘一致；桌面内容最大宽度为 1160px。
- 390px 和 375px 宽度下，第一张卡片的展台约从页面顶部 391px 开始；844px 高度的首屏可读到卡名和辅助资料。
- 逐张加载、解码并查看真实图片，检查浅色、深色、竖版和长名称素材。展台比例一致，卡面等比完整展示，长名称可自然换行。用户随后补充要求保留银行卡式圆角，现已改为列表卡面 10px、大图卡面 16px；圆角贴合图片实际边缘，覆盖原方案中不强加卡面圆角的要求。
- 悬停时仅卡面上移 3px，卡名和相邻展台位置不变。触屏模拟下不触发悬停位移。
- 搜索 `bilibili`、卡种、银行、卡组织组合筛选，以及清空搜索、零结果提示和重置均通过。
- `/` 聚焦搜索；筛选菜单方向键、Home、End、Enter、Esc、Tab 和点击外部关闭通过，键盘焦点清晰可见。
- 大图按钮、Esc 和点击遮罩均可关闭弹层，关闭后焦点返回原卡片。各尺寸面板距边缘至少 12px，关闭按钮为 44 × 44px。
- 使用只存在于浏览器测试中的超长标题检查：标题完整换行，内容区可滚动，滚到末尾时关闭区仍可见。测试没有写入卡片数据。
- 开启减少动态效果后，卡面位移、过渡和弹层淡入均停用。
- 原有图片加载失败、空收藏和卡片数据加载失败状态均通过浏览器临时请求模拟检查。
- 浏览器运行期间未捕获 JavaScript 异常。

## 200% 原生页面缩放

使用独立临时 Chrome 配置启用浏览器的 200% 页面缩放，验证实际 `devicePixelRatio = 2` 和布局视口减半；不是只调整截图尺寸。该配置未写入日常浏览器资料。

| 浏览器视口设置 | 缩放后的 CSS 视口 | 列数 | 结果 |
| --- | --- | --- | --- |
| 1440 × 900 | 720 × 450 | 2 | 控件无重叠，页面与弹层无横向溢出，关闭与焦点返回正常。 |
| 1024 × 900 | 512 × 450 | 1 | 同上。 |
| 768 × 900 | 384 × 450 | 1 | 同上。 |

## 素材与验证边界

- `锦鲤主题储蓄卡` 原图是带大量背景的竖版实拍。完整展示后，实际银行卡在展台中比其他卡面小；按本轮要求保留原素材，没有裁切或放大隐藏背景。
- 本轮验证覆盖 Linux Chrome 和触屏模拟；未在真实 iOS/Android 设备、Safari、Firefox、Windows 或 macOS 上验证。本地字体回退在不同系统中的实际字形仍有差异。
- 验收针对本地构建，不代表已部署到线上。

本地截图、浏览器检查脚本和详细结果保存在已被 Git 忽略的 `output/visual-redesign/` 中，其中 `first-screen-1440.png`、`first-screen-390.png` 为首屏预览，`viewport-768.png` 为平板完整页面，`dialog-390.png` 为手机弹层。输出不进入发布目录。

## 圆角补充验收

按用户后续要求恢复卡面圆角后，重新检查了 1440px、768px、390px 三种宽度下的全部 9 张图片：圆角贴合图片边缘，原图比例、显示尺寸、居中位置均正常，无横向溢出。横版和竖版大图的圆角、关闭后焦点返回，以及卡面悬停和减少动态效果检查通过。`node --check app.js` 与 `git diff --check` 通过。

更新后的预览为 `rounded-first-screen-1440.png`、`rounded-first-screen-390.png` 和 `rounded-dialog-390.png`；详细结果为 `rounded-report.json`，均位于上述本地输出目录。

## webapps 风格补充验收

2026-09-18，按用户最新要求，将同级 webapps 项目当前 `src/styles.css` 和 `uno.config.ts` 中的颜色、字体、圆角与面板风格应用到 card。

- 页面底色为 `#f6f7f9`，展台、搜索、筛选菜单与详情面板为白色，主要文字为 `#182230`，辅助文字为 `#526075`，边框为 `#e2e6ec`。
- 强调色采用 `#2563eb`，选中状态采用 `#eff4ff` 配 `#1d4ed8`；主操作按钮为蓝底白字。标题改为无衬线字体，以蓝色强调“拾光”，浏览器主题色同步更新。
- 控件圆角为 8px，展台与弹层圆角为 16px；白色展台增加细边框与轻阴影，详情卡面底色为 `#f1f3f6`。卡面原比例、列表 10px 圆角与详情 16px 圆角保持一致。
- 在 1024px 宽度下发现一张长卡名的全角括号造成 6px 横向溢出；为卡名与详情标题保留完整全角标点间距后，重新检查全部响应式尺寸，换行与边界均正常。
- 本次只修改 `styles.css`、`index.html` 和视觉文档，卡片资料与业务交互保持原有实现。

| 检查 | 本轮结果 |
| --- | --- |
| `pnpm check:cards` | 15 张卡片资料通过。 |
| `pnpm test` | 33 项通过，0 失败。 |
| `pnpm build` | 15 张本地卡面构建成功，生成 `public/`。 |
| 浏览器布局 | 1440、1024、1023、768、600、599、390、375、320px 均通过；长名称完整换行，页面、控件与弹层无横向溢出。 |
| 图片检查 | 15 张真实卡面全部解码成功，保持原始比例，完整位于展台中。 |
| 交互检查 | 搜索、组合筛选、清空与重置、空结果、菜单键盘操作、弹层关闭与焦点返回、触屏模拟和减少动态效果均通过；无 JavaScript 异常。 |
| 200% 浏览器原生缩放 | 1440、1024、768px 物理视口分别得到 720、512、384px CSS 视口；控件无重叠，页面与弹层无横向溢出。 |
| axe 自动检查 | 桌面、手机、银行菜单、卡组织菜单、详情弹层和空结果共 6 个状态，均为 0 项自动判定违规。菜单在淡入结束后采样；需要人工判断的 `aria-controls` 引用与装饰符号项保存在报告中。 |
| `git diff --check` | 通过。 |

本轮使用 Linux Chrome 150.0.7871.128 与 Playwright 验证本地构建；手机检查使用浏览器模拟。未据此宣称其他浏览器、真实移动设备或线上部署已通过验收。

当前截图和报告位于 Git 忽略的 `output/webapps-theme/`：`first-screen-1440.png`、`first-screen-390.png`、`dialog-390.png`、`report.json`、`accessibility.json`；检查脚本为 `check.mjs` 与 `check-zoom.mjs`。这些文件不进入发布目录。
